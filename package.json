{
	"scripts": {
		"lerna": "lerna bootstrap",
		"build": "lerna run build --stream",
		"clean": "lerna clean",
		"publish": "lerna publish patch --no-private"
	},
	"devDependencies": {
		"lerna": "5.5.0"
	}
}
