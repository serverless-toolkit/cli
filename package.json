{
	"scripts": {
		"lerna": "lerna bootstrap",
		"build": "lerna run build --stream",
		"clean": "lerna clean",
		"publish": "lerna version --no-private && lerna publish --no-private"
	},
	"devDependencies": {
		"lerna": "5.5.0"
	}
}
