{
	"scripts": {
		"lerna": "lerna bootstrap",
		"build": "lerna run build --stream",
		"clean": "lerna clean",
		"publish": "npm run build && lerna publish patch --no-private"
	},
	"devDependencies": {
		"lerna": "5.5.0"
	}
}
